cmake_minimum_required(VERSION 2.8)
if( UNIX AND NOT APPLE )
    add_library( ui ui_unix.cpp etbapplication.cpp )
    if(WITH_LOCAL_OPENCV)
        add_dependencies( ui opencv_local )
    endif()
elseif(APPLE)
    message( STATUS "No UI  support for MacOSX yet" )
endif( )
